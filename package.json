{
  "name": "infinicanvas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Visual communication protocol for humans and AI — a shared infinite canvas for co-creation",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "INFINICANVAS_API_KEY=local-dev INFINICANVAS_SESSION_ID=local-dev concurrently -n gateway,app -c blue,green \"npm run start -w @infinicanvas/gateway\" \"npm run dev -w @infinicanvas/app\"",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
